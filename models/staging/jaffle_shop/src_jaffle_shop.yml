version: 2

sources:
  - name: jaffle_shop
    database: DEMO_DB  
    tables:
      - name: customers
        columns:
          - name: ID
            tests: 
              - unique
              - not_null
      - name: orders
        columns:
          - name: ID
            tests:
              - unique
              - not_null